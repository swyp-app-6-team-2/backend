# Ingredient Master — Design Spec

**Status:** Approved · 2026-09-09
**Issue:** [#20 Ingredient 마스터 DB 구축](https://github.com/swyp-app-6-team-2/backend/issues/20)

이 문서는 재료 마스터 도메인의 **계약**을 소유한다. 구현 계획(파일 목록·커밋 단계·테스트 코드)은 이 문서를 근거로 별도 plan 문서가 다룬다.

---

## 1. Goal

PM팀이 확정한 재료 87개를 마스터 데이터로 적재하고, 앱이 조회할 수 있는 API를 제공하며, Recipe가 자유 입력 재료명 대신 이 마스터를 참조할 수 있게 한다.

## 2. Non-Goals

이 범위에 **넣지 않는다.** 아래를 추가하는 것은 범위 초과다.

| 제외 | 이유 |
|---|---|
| 재료 아이콘 컬럼·URL | 전달 방식 미정. [#24](https://github.com/swyp-app-6-team-2/backend/issues/24) |
| 사용자 보유 재료(`user_ingredient`), 유통기한, 냉장고 필터 | Discovery 도메인 소유 |
| 재료 CRUD API (생성·수정·삭제) | 마스터는 migration으로만 바뀐다 |
| 서버 측 재료 검색·필터·페이지네이션 파라미터 | 87개라 클라이언트가 거른다 (§9.5) |
| 재료 계층 구조(`parent_id`), 대체 재료, 영양 정보 | 요구가 없다 |
| 재료명 정규화·형태소 분석·검색엔진 | 규모가 안 된다 |

## 3. Architecture Overview

`Ingredient`는 **독립 도메인**이다. Recipe와 Discovery가 함께 참조하는 공용 마스터이므로 Recipe 안에 두지 않는다.

읽기 전용 참조 데이터다. 쓰기 경로가 없고, 트랜잭션·동시성 제어가 필요 없으며, 데이터는 Flyway migration이 소유한다.

```
[앱] --GET /api/v1/ingredients--> IngredientController -> IngredientService -> IngredientRepository
                                                                                      |
[앱] --POST/PATCH /api/v1/recipes--> RecipeController -> RecipeService --existsAll()---+
                                                              |
                                                    recipe_ingredient.ingredient_id (FK)
```

Recipe는 `IngredientRepository`나 `Ingredient` Entity를 직접 참조하지 않고 `IngredientService`가 공개한 메서드만 호출한다.

## 4. Global Constraints

플랜과 구현이 그대로 따라야 하는 프로젝트 전역 제약이다. 값은 실제 저장소에서 확인한 것이다.

- Java 25 · Spring Boot 4.1.1 · Hibernate ORM 7.4.5 · PostgreSQL 18 · PostgreSQL JDBC 42.7.13 · Flyway 12.4.0
- Gradle Wrapper 9.7.1, 단일 모듈. 빌드 명령은 `./gradlew`
- **DB 스키마는 `src/main/resources/db/migration`의 Flyway migration이 소유한다.** Hibernate는 `ddl-auto: validate`다. Entity만 고치면 기동이 막힌다
- **이미 적용된 migration 파일은 수정하지 않는다.** 새 `V{n}__<설명>.sql`을 추가한다
- `validate`는 컬럼 존재와 타입만 본다. **nullable·UNIQUE·인덱스·CHECK·FK는 검사하지 않는다.** 도메인별 `*SchemaTest`가 `information_schema`를 직접 조회해 확인한다
- enum 값을 고정한 CHECK 제약은 `EnumCheckConstraintTest`가 지킨다. **새 `ck_` 제약을 만들면 그 테스트의 `CONSTRAINTS` 목록에 등록해야 한다.** 등록하지 않으면 테스트가 실패한다
- 통합 테스트는 `@SpringBootTest`가 아니라 **`@IntegrationTest`**(`com.star_pick.starpick.support`)를 쓴다. Testcontainers가 PostgreSQL 18을 띄우고 Flyway가 스키마를 만든다
- 테스트 의존성은 이미 클래스패스에 있다(JUnit Jupiter 6.0.3, AssertJ, Mockito, spring-test). **build.gradle에 추가하지 않는다**
- `./gradlew test`는 환경변수도 수동 DB도 요구하지 않는다. Docker daemon만 있으면 통과해야 한다
- Base path `/api/v1`. JSON key는 `camelCase`. 목록이 비면 `null`이 아니라 `[]`
- 인증 API 외 모든 API는 인증이 필요하다. `SecurityConfig`가 `anyRequest().authenticated()`이므로 **새 endpoint에 별도 설정이 필요 없다**
- 공통 응답 Envelope는 `ApiResponse<T>`(`status`, `message`, `data`). 성공은 `ApiResponse.ok(message, data)`. 실패의 machine-readable code는 `data.code`
- ErrorCode는 도메인별 enum이 `com.star_pick.starpick.global.exception.ErrorCode`를 구현하고, **enum 상수명이 그대로 공개 code가 된다**. 형식은 `{RESOURCE_OR_CONTEXT}_{REASON}`
- Controller는 `@AuthenticationPrincipal AuthenticatedUser user`로 사용자를 받는다(`record AuthenticatedUser(Long userId)`)
- wildcard import를 쓰지 않는다. 외부 HTTP는 `RestClient`만 쓴다
- Lombok을 쓰되 Entity의 불변조건을 우회하는 광범위한 setter를 만들지 않는다
- 커밋 형식은 `type: 내용`(`feat`, `fix`, `refactor`, `perf`, `docs`, `test`, `chore`, `style`, `ci`). **커밋 메시지에 trailer(`Co-Authored-By` 등)를 넣지 않는다**
- **커밋은 사용자가 직접 실행한다.** 에이전트는 메시지만 제안한다

---

## 5. Data Model

### 5.1. `ingredient`

```sql
create table ingredient
(
    id            bigint generated by default as identity,
    code          varchar(255) not null,
    name          varchar(255) not null,
    category_code varchar(255) not null,
    aliases       text[]       not null default '{}',
    active        boolean      not null default true,
    constraint pk_ingredient primary key (id),
    constraint uk_ingredient_code unique (code),
    constraint ck_ingredient_category_code
        check (category_code in ('MEAT', 'SEAFOOD', 'VEGETABLE', 'SAUCE', 'ETC'))
);
```

| 컬럼 | 필수 | 규칙 |
|---|---|---|
| `id` | O | DB·API가 쓰는 식별자. `recipe_ingredient.ingredient_id`가 참조 |
| `code` | O | PM 시트의 `ingre_id`(`MET001`). 영구 번호이며 재사용·재번호를 하지 않는다. 접두사를 파싱하지 않는다 — 분류의 진실은 `category_code`다 |
| `name` | O | 화면에 표시하는 재료명 |
| `category_code` | O | `MEAT`, `SEAFOOD`, `VEGETABLE`, `SAUCE`, `ETC` |
| `aliases` | O | 검색어 배열. 없으면 빈 배열 `{}`이며 `null`이 아니다. **표시명이 아니다** — 화면에 노출하지 않는다 |
| `active` | O | `false`면 조회 API 목록에서 제외된다. 기본 `true` |

`created_at`/`updated_at`을 두지 않는다. 데이터가 migration으로만 바뀌고, `recipe_ingredient`·`recipe_step`·`cook_history`·`upload_object`도 두고 있지 않다.

`display_order`를 두지 않는다. 정렬은 §7.3이 정의한다.

별칭 저장에 별도 테이블을 두지 않는다. 근거는 §10.2.

**`text[]` 매핑은 검증됐다.** 2026-09-09에 버릴 스파이크로 `@JdbcTypeCode(SqlTypes.ARRAY)` + `String[]` + `text[]` 컬럼이 이 저장소의 Hibernate 7.4.5 · PostgreSQL 18 · `ddl-auto: validate` 조합에서 기동과 한글 값 왕복을 통과하는 것을 확인했다.

### 5.2. `recipe_ingredient` FK

`recipe_ingredient.ingredient_id` 컬럼은 이미 있다(V1, `bigint` nullable, FK 없음). 참조할 테이블이 생겼으므로 FK를 추가한다.

```sql
alter table recipe_ingredient
    add constraint fk_recipe_ingredient_ingredient
        foreign key (ingredient_id) references ingredient (id);
```

기존 `recipe_ingredient` 행의 `ingredient_id`는 전부 `null`이라 제약 추가가 실패하지 않는다.

### 5.3. Migration 파일 분리

마스터 스키마·시드와 `recipe_ingredient` FK를 **다른 migration 파일로 나눈다.** 전자는 Ingredient 도메인만의 변경이고 후자는 Recipe 계약 변경이라 커밋 경계가 다르다.

현재 적용된 마지막 migration은 `V1__init.sql`이다. 따라서 §5.1·§6이 `V2`, §5.2가 `V3`가 된다.

---

## 6. Seed Data

출처는 PM팀 Google Sheets(`1XX4-YRimYD-ppcCDdcYMOF8O1oY3C8rGvPQbySPByW4`, gid `1442157040`), 2026-09-07 전달. 87행이며 카테고리별로 육류 12 / 해산물 11 / 채소 24 / 소스류 22 / 기타 18이다.

`name`은 시트의 `ingre_name`을 **그대로** 쓴다. 괄호·슬래시 표기(`돼지고기(삼겹살)`, `파프리카/피망`)를 다듬지 않는다 — 이름은 PM·디자인팀 소유이고, 표시명을 줄일지는 [#22](https://github.com/swyp-app-6-team-2/backend/issues/22)에서 논의 중이다(§11).

별칭은 **`name` 안에 이미 들어 있는 표기를 쪼갠 것**과 명백한 이표기(`쇠고기`→`소고기`)뿐이다. 13개 재료에만 있고 나머지 74개는 빈 배열이다. **없는 별칭을 상상해서 채우지 않는다.**

```sql
insert into ingredient (code, name, category_code, aliases) values
('MET001', '돼지고기(삼겹살)',      'MEAT',      '{"삼겹살","돼지고기"}'),
('MET002', '돼지고기(목살)',        'MEAT',      '{"목살","돼지고기"}'),
('MET003', '돼지고기(다짐육)',      'MEAT',      '{"다짐육","돼지고기","다진돼지고기"}'),
('MET004', '돼지고기(앞다리살)',    'MEAT',      '{"앞다리살","돼지고기"}'),
('MET005', '쇠고기(다짐육)',        'MEAT',      '{"다짐육","쇠고기","소고기","다진소고기"}'),
('MET006', '쇠고기(등심/불고기용)', 'MEAT',      '{"등심","불고기용","쇠고기","소고기"}'),
('MET007', '쇠고기(우삼겹)',        'MEAT',      '{"우삼겹","쇠고기","소고기"}'),
('MET008', '닭가슴살',              'MEAT',      '{}'),
('MET009', '닭다리살',              'MEAT',      '{}'),
('MET010', '오리고기',              'MEAT',      '{}'),
('MET011', '베이컨',                'MEAT',      '{}'),
('MET012', '소시지/햄',             'MEAT',      '{"소시지","햄"}'),
('SEA001', '고등어',                'SEAFOOD',   '{}'),
('SEA002', '연어',                  'SEAFOOD',   '{}'),
('SEA003', '갈치',                  'SEAFOOD',   '{}'),
('SEA004', '명태/동태',             'SEAFOOD',   '{"명태","동태"}'),
('SEA005', '참치(캔)',              'SEAFOOD',   '{"참치","참치캔"}'),
('SEA006', '새우',                  'SEAFOOD',   '{}'),
('SEA007', '오징어',                'SEAFOOD',   '{}'),
('SEA008', '바지락',                'SEAFOOD',   '{}'),
('SEA009', '홍합',                  'SEAFOOD',   '{}'),
('SEA010', '어묵',                  'SEAFOOD',   '{}'),
('SEA011', '멸치(건멸치)',          'SEAFOOD',   '{"멸치","건멸치"}'),
('VEG001', '양파',                  'VEGETABLE', '{}'),
('VEG002', '대파',                  'VEGETABLE', '{}'),
('VEG003', '마늘',                  'VEGETABLE', '{}'),
('VEG004', '생강',                  'VEGETABLE', '{}'),
('VEG005', '청양고추',              'VEGETABLE', '{}'),
('VEG006', '홍고추',                'VEGETABLE', '{}'),
('VEG007', '감자',                  'VEGETABLE', '{}'),
('VEG008', '고구마',                'VEGETABLE', '{}'),
('VEG009', '당근',                  'VEGETABLE', '{}'),
('VEG010', '무',                    'VEGETABLE', '{}'),
('VEG011', '배추',                  'VEGETABLE', '{}'),
('VEG012', '상추',                  'VEGETABLE', '{}'),
('VEG013', '깻잎',                  'VEGETABLE', '{}'),
('VEG014', '시금치',                'VEGETABLE', '{}'),
('VEG015', '애호박',                'VEGETABLE', '{}'),
('VEG016', '오이',                  'VEGETABLE', '{}'),
('VEG017', '토마토',                'VEGETABLE', '{}'),
('VEG018', '파프리카/피망',         'VEGETABLE', '{"파프리카","피망"}'),
('VEG019', '콩나물',                'VEGETABLE', '{}'),
('VEG020', '숙주나물',              'VEGETABLE', '{}'),
('VEG021', '버섯',                  'VEGETABLE', '{}'),
('VEG022', '양배추',                'VEGETABLE', '{}'),
('VEG023', '브로콜리',              'VEGETABLE', '{}'),
('VEG024', '옥수수',                'VEGETABLE', '{}'),
('SAU001', '진간장',                'SAUCE',     '{}'),
('SAU002', '국간장',                'SAUCE',     '{}'),
('SAU003', '고추장',                'SAUCE',     '{}'),
('SAU004', '된장',                  'SAUCE',     '{}'),
('SAU005', '쌈장',                  'SAUCE',     '{}'),
('SAU006', '굴소스',                'SAUCE',     '{}'),
('SAU007', '케첩',                  'SAUCE',     '{}'),
('SAU008', '마요네즈',              'SAUCE',     '{}'),
('SAU009', '머스타드',              'SAUCE',     '{}'),
('SAU010', '돈가스소스',            'SAUCE',     '{}'),
('SAU011', '스리라차',              'SAUCE',     '{}'),
('SAU012', '액젓',                  'SAUCE',     '{}'),
('SAU013', '식초',                  'SAUCE',     '{}'),
('SAU014', '맛술(미림)',            'SAUCE',     '{"맛술","미림"}'),
('SAU015', '식용유',                'SAUCE',     '{}'),
('SAU016', '참기름',                'SAUCE',     '{}'),
('SAU017', '들기름',                'SAUCE',     '{}'),
('SAU018', '올리브오일',            'SAUCE',     '{}'),
('SAU019', '설탕',                  'SAUCE',     '{}'),
('SAU020', '소금',                  'SAUCE',     '{}'),
('SAU021', '후추',                  'SAUCE',     '{}'),
('SAU022', '고춧가루',              'SAUCE',     '{}'),
('ETC001', '계란',                  'ETC',       '{}'),
('ETC002', '우유',                  'ETC',       '{}'),
('ETC003', '치즈',                  'ETC',       '{}'),
('ETC004', '버터',                  'ETC',       '{}'),
('ETC005', '생크림',                'ETC',       '{}'),
('ETC006', '요거트',                'ETC',       '{}'),
('ETC007', '쌀',                    'ETC',       '{}'),
('ETC008', '밀가루',                'ETC',       '{}'),
('ETC009', '소면',                  'ETC',       '{}'),
('ETC010', '당면',                  'ETC',       '{}'),
('ETC011', '파스타면',              'ETC',       '{}'),
('ETC012', '식빵',                  'ETC',       '{}'),
('ETC013', '두부',                  'ETC',       '{}'),
('ETC014', '다시마',                'ETC',       '{}'),
('ETC015', '가쓰오부시',            'ETC',       '{}'),
('ETC016', '참깨',                  'ETC',       '{}'),
('ETC017', '김',                    'ETC',       '{}'),
('ETC018', '김치',                  'ETC',       '{}');
```

---

## 7. API — `GET /api/v1/ingredients`

활성 재료 전체를 한 번에 반환한다.

### 7.1. 요청

파라미터가 없다. 검색어·카테고리 필터·페이지네이션을 받지 않는다.

```http
GET /api/v1/ingredients
Authorization: Bearer {accessToken}
```

### 7.2. 응답 `200 OK`

```json
{
  "status": 200,
  "message": "재료 목록을 조회했습니다.",
  "data": {
    "ingredients": [
      {
        "ingredientId": 1,
        "code": "MET001",
        "name": "돼지고기(삼겹살)",
        "categoryCode": "MEAT",
        "aliases": ["돼지고기", "삼겹살"]
      },
      {
        "ingredientId": 8,
        "code": "MET008",
        "name": "닭가슴살",
        "categoryCode": "MEAT",
        "aliases": []
      }
    ]
  }
}
```

| 필드 | 타입 | 설명 |
|---|---|---|
| `ingredientId` | number | `ingredient.id`. Recipe 저장 시 이 값을 보낸다 |
| `code` | string | `MET001`. 앱이 아이콘을 찾는 키다([#24](https://github.com/swyp-app-6-team-2/backend/issues/24)) |
| `name` | string | 표시명 |
| `categoryCode` | string | `MEAT` \| `SEAFOOD` \| `VEGETABLE` \| `SAUCE` \| `ETC` |
| `aliases` | string[] | 검색어. 없으면 `[]` (`null` 아님) |

응답에 `active`를 넣지 않는다 — 비활성은 목록에서 아예 빠지므로 항상 `true`가 되어 정보가 없다.

카테고리 한글 라벨(`육류` 등)을 내려주지 않는다. 화면 문구는 앱이 소유한다.

### 7.3. 정렬

서버가 정렬해서 내려준다. 앱은 배열 순서를 그대로 쓴다.

1. **카테고리** — `IngredientCategory` enum **선언 순서**: `MEAT` → `SEAFOOD` → `VEGETABLE` → `SAUCE` → `ETC`
2. 같은 카테고리 안에서는 **`code` 오름차순**

`category_code` 문자열을 그대로 정렬하면 `ETC`가 맨 앞에 온다. 그건 PM 시트의 순서가 아니다. **enum 선언 순서로 정렬해야 한다.**

`aliases` 배열의 순서는 정의하지 않는다. 검색용이라 순서에 의미가 없다.

### 7.4. 실패

| 조건 | 응답 |
|---|---|
| 인증 헤더 없음·토큰 무효 | `401` + `AUTHENTICATION_REQUIRED` (Security 공통 처리) |

이 API 고유의 실패가 없다. 목록이 비어도 `200` + `"ingredients": []`다.

---

## 8. Recipe Integration

### 8.1. 요청 — `POST` / `PATCH /api/v1/recipes`

`RecipeIngredientRequest`에 **`ingredientId`(선택)를 추가한다.** 나머지는 그대로다.

```json
"ingredients": [
  { "ingredientId": 1, "name": "돼지고기(삼겹살)", "amountText": "300g" },
  { "name": "트러플오일", "amountText": "약간" }
]
```

- `ingredientId` — 선택. 마스터에서 고른 재료면 보낸다. 직접 입력이면 생략하거나 `null`
- `name` — **여전히 필수다.** `@NotBlank`, 최대 255자. `ingredientId`가 있어도 보내야 한다
- `amountText` — 선택. 최대 255자

`ingredientId`에 Bean Validation 애노테이션을 붙이지 않는다. 존재 여부는 형식이 아니라 도메인 검증이다.

**서버는 `name`을 마스터 값으로 덮어쓰지 않는다.** 상세는 §10.3.

### 8.2. 응답 — `GET /api/v1/recipes/{recipeId}`

`RecipeIngredientResponse`에 **`ingredientId`를 추가한다.**

```json
"ingredients": [
  { "ingredientId": 1, "name": "돼지고기(삼겹살)", "amountText": "300g" },
  { "ingredientId": null, "name": "트러플오일", "amountText": "약간" }
]
```

**이것은 기존 계약의 변경이다.** `docs/specs/recipe.md`와 로컬 BE 문서 `02-1 Recipe API`가 *"Ingredient와 Step의 내부 식별자와 표시 순서는 노출하지 않는다"* 라고 적고 있는데, 그 문장이 가리키는 것은 `RecipeIngredient`의 **PK와 `displayOrder`**다. 그 둘은 계속 노출하지 않는다. `ingredientId`는 내부 식별자가 아니라 **마스터 참조**이고 노출해야 한다 — 이유는 §10.4.

### 8.3. 검증

요청의 `ingredientId` 중 `null`이 아닌 값을 모아 **중복을 제거**하고, 모두 `ingredient` 테이블에 존재하는지 확인한다. 하나라도 없으면 전체 요청을 실패시킨다.

| 조건 | 응답 |
|---|---|
| 존재하지 않는 `ingredientId`가 하나 이상 | `400` + `RECIPE_INGREDIENT_INVALID` |

```json
{
  "status": 400,
  "message": "선택한 재료를 찾을 수 없습니다.",
  "data": { "code": "RECIPE_INGREDIENT_INVALID" }
}
```

**`active`를 검증하지 않는다.** 비활성 재료의 `ingredientId`를 보내도 성공한다. 이유는 §10.5.

**같은 `ingredientId`가 여러 번 나와도 허용한다.** "삼겹살 300g", "삼겹살 100g"처럼 나눠 적을 수 있다.

`RECIPE_INGREDIENT_INVALID`는 `RecipeErrorCode`에 추가한다. Ingredient 도메인이 아니라 Recipe 도메인의 실패다 — Recipe 요청의 값이 잘못된 것이기 때문이다.

### 8.4. PATCH 전체 교체

`ingredients` 배열의 의미는 기존과 같다. 미전달이면 유지, `[]`면 전체 삭제, 값이 있으면 전체 교체.

교체 시 **`ingredientId`도 변경 비교 대상에 포함한다.** `RecipeIngredient.hasSameContentAs`가 `name`·`amountText`만 비교하면, 이름과 수량이 같고 `ingredientId`만 다른 요청이 "변경 없음"으로 판정되어 마스터 연결이 갱신되지 않는다.

---

## 9. Components & Placement

### 9.1. 새로 만드는 것 — `com.star_pick.starpick.domain.ingredient`

```
domain/ingredient
├── controller
│   ├── IngredientController
│   └── response/IngredientListResponse
├── domain
│   ├── Ingredient
│   └── IngredientCategory
├── repository
│   └── IngredientRepository
└── service
    └── IngredientService
```

`exception` 패키지를 만들지 않는다. 이 도메인이 던지는 실패가 없다.

### 9.2. `IngredientCategory`

```java
public enum IngredientCategory { MEAT, SEAFOOD, VEGETABLE, SAUCE, ETC }
```

**선언 순서가 곧 화면 표시 순서다**(§7.3). 순서를 바꾸면 앱의 카테고리 배열이 바뀐다.

상수를 추가·변경하면 `ck_ingredient_category_code`를 갱신하는 migration이 함께 필요하고, `EnumCheckConstraintTest`에도 등록해야 한다.

### 9.3. `Ingredient`

`id`, `code`, `name`, `category`(`@Enumerated(STRING)`, 컬럼 `category_code`), `aliases`, `active`.

`aliases`는 **`@JdbcTypeCode(SqlTypes.ARRAY)` + `String[]`** 로 `text[]` 컬럼에 매핑한다(§5.1에서 검증). 별도 테이블로 바꾸지 않는다 — 이유는 §10.2.

읽기 전용이다. 상태 변경 메서드도 setter도 만들지 않는다.

### 9.4. `IngredientService`

두 가지만 공개한다.

| 메서드 | 용도 | 계약 |
|---|---|---|
| `IngredientListResponse findActiveIngredients()` | 조회 API | `active = true`인 재료를 §7.3 순서로. 각 재료의 `aliases` 포함 |
| `boolean existsAll(Collection<Long> ingredientIds)` | Recipe 검증 | 주어진 id가 **모두 존재**하면 `true`. **`active`를 보지 않는다.** 빈 컬렉션은 `true` |

`existsAll`은 도메인 경계를 넘는 유일한 창구다. Recipe는 이 메서드만 호출하고 `IngredientRepository`·`Ingredient`를 직접 쓰지 않는다.

`existsAll`은 Ingredient 도메인의 예외를 던지지 않고 `boolean`을 반환한다. 실패를 어떤 ErrorCode로 표현할지는 호출자인 Recipe가 정한다.

### 9.5. 조회 성능

87행짜리 단일 테이블 조회다. `aliases`가 같은 행의 컬럼이라 조인도 `fetch join`도 N+1도 없다.

§7.3 정렬은 조회한 뒤 애플리케이션에서 수행한다. enum 선언 순서는 SQL `ORDER BY`로 표현할 수 없다.

캐시를 두지 않는다. 이 규모에서 캐시는 무효화 비용만 늘린다.

### 9.6. 고치는 것 — Recipe

| 대상 | 변경 |
|---|---|
| `RecipeIngredientRequest` | `Long ingredientId` 필드 추가 |
| `RecipeDetailResponse.RecipeIngredientResponse` | `Long ingredientId` 필드 추가 |
| `RecipeIngredient` | `ingredientId`를 생성 시점에 받도록 팩토리 변경. `hasSameContentAs`에 `ingredientId` 비교 추가 |
| `RecipeService` | 생성·수정에서 `ingredientService.existsAll(...)` 호출, 실패 시 `RECIPE_INGREDIENT_INVALID` |
| `RecipeErrorCode` | `RECIPE_INGREDIENT_INVALID(BAD_REQUEST, "선택한 재료를 찾을 수 없습니다.")` 추가 |

---

## 10. Design Decisions

각 항목은 **채택안 / 검토했지만 기각한 대안 / 기각 이유** 순이다.

### 10.1. 식별자는 `bigint id` PK + `code` UNIQUE

`code`(`MET001`)를 PK로 쓰지 않는다.

- **기각한 대안:** `code`를 PK로 두고 `recipe_ingredient.ingredient_id`를 `varchar`로 변경
- **기각 이유:** `recipe_ingredient.ingredient_id`가 이미 `bigint`로 존재하고, 저장소의 모든 FK가 `bigint`다. 얻는 것(API에 사람이 읽는 식별자)보다 잃는 것(타입 변경 migration, 관례 이탈)이 크다
- **기각한 대안:** `code` 없이 `id`만 두고 `name`을 자연키로 사용
- **기각 이유:** PM이 `ingre_id`를 영구 번호로 부여했고 재사용·재번호를 금지했다. 반대로 `name`은 [#22](https://github.com/swyp-app-6-team-2/backend/issues/22)에서 바뀔 수 있다. 이름을 키로 쓰면 표기 정리가 행 삭제+생성이 되어 참조가 끊긴다

### 10.2. 별칭은 `text[]` 컬럼

**별도 테이블로 바꾸지 말 것.**

- **채택 이유:** 별칭은 이 도메인에서 **한 재료에 딸린 값 목록**일 뿐이다. 자체 식별자도, 생명주기도, 다른 곳에서의 참조도 없다. 컬럼 하나면 테이블·조인·`fetch join`이 모두 사라지고, 시드도 `ingredient` INSERT 한 번에 끝난다
- **기각한 대안:** `ingredient_alias(ingredient_id, alias)` 테이블 + `@ElementCollection`
- **기각 이유:** 정규화가 관계형의 기본값이지만, 여기서 얻는 것은 `UNIQUE(ingredient_id, alias)`와 DB 측 별칭 조회 두 가지다. 전자는 시드가 통제돼 있어 막을 중복이 없고, 후자는 §2에서 서버 측 재료 검색을 명시적으로 범위 밖에 뒀다. 대가로 테이블 하나, 별도 시드 32행, 조회 경로의 `fetch join`이 늘어난다
- **기각한 대안:** 쉼표로 구분한 `text` 한 컬럼(식품의약품안전처 성분코드 API의 `이명` 필드 방식)
- **기각 이유:** 분리·결합 코드가 애플리케이션에 들어가고, 별칭에 쉼표를 쓸 수 없게 되며, DB가 배열 원소를 다루지 못한다

**`text[]`와 `ddl-auto: validate`의 호환성은 추정이 아니라 실측이다.** 이 결정의 초안은 "PostgreSQL 배열 컬럼이 `validate`와 충돌한다"는 이유로 별도 테이블을 택했으나, 그 근거는 Hibernate 6.3~6.4 시절 `@Type(ListArrayType)` 방식에 대한 보고였고 이 저장소에는 해당하지 않는다. 2026-09-09에 버릴 스파이크로 직접 확인했다(§5.1). 이 결정을 다시 뒤집으려면 같은 방식으로 반증해야 한다.

- **서버 측 별칭 검색이 필요해지면:** PostgreSQL은 `'삼겹살' = ANY(aliases)`와 GIN 인덱스로 배열 검색을 지원한다. 테이블로 옮기지 않아도 된다

### 10.3. `name`은 앱이 보낸 값을 그대로 저장

**서버가 마스터에서 이름을 복사해 덮어쓰지 말 것.**

- **기각한 대안:** `ingredientId`가 있으면 서버가 `ingredient.name`을 `recipe_ingredient.name`에 복사
- **기각 이유:** 기준 문서가 `RecipeIngredient.name`을 *"Recipe 저장 당시 **사용자가 확인한** 재료명 스냅샷"*, *"`Ingredient.name`을 동적으로 조회한 값이 아니다"* 로 정의한다. 사용자가 화면에서 본 글자를 남기는 것이 목적이므로 그 값을 아는 쪽은 앱이다. [#22](https://github.com/swyp-app-6-team-2/backend/issues/22)로 마스터 이름이 바뀐 뒤 앱 캐시가 낡은 상태에서 저장하면, 덮어쓰기는 사용자가 본 적 없는 이름을 저장하게 된다
- 두 값이 어긋나도 기능이 깨지지 않는다. 냉장고 필터 매칭은 `ingredient_id`로 하고 `name`은 표시용이라 역할이 분리돼 있다

### 10.4. 조회 응답에 `ingredientId`를 노출

- **기각한 대안:** 기존 문서대로 `ingredientId`를 노출하지 않음
- **기각 이유:** `PATCH`가 재료 배열을 **전체 교체**한다. 앱이 재료 하나를 추가하려면 기존 재료를 전부 다시 보내야 하는데, 조회 응답에 `ingredientId`가 없으면 다시 보낼 수 없다. 그러면 삼겹살로 저장한 레시피를 제목만 고쳐도 모든 재료가 자유 입력으로 바뀐다. 노출하지 않으면 마스터 연결이 첫 수정에서 소멸한다

### 10.5. 검증은 존재만, `active`는 보지 않음

**활성 검증을 추가하지 말 것.**

- **기각한 대안:** `ingredientId`가 존재하고 `active = true`인지 함께 검증
- **기각 이유:** `PATCH`가 전체 교체다. 어떤 재료가 비활성이 된 뒤 그 재료를 쓰던 레시피를 수정하려면 비활성 `ingredientId`를 다시 보내야 하는데, 활성까지 검증하면 **그 레시피를 영영 수정할 수 없게 된다**
- **기각한 대안:** "새로 붙는 `ingredientId`만 활성 검증"(기존 연결은 통과)
- **기각 이유:** 위 문제는 풀리지만 기존 재료 집합과의 차집합 계산이 필요하다. 그렇게 막아서 얻는 것은 "목록을 캐시한 낡은 앱이 비활성 재료를 새로 붙이는 것"을 차단하는 정도이고, 그래도 데이터가 깨지지 않는다(FK는 유효하다). 비용 대비 이득이 없다
- 따라서 `active`의 의미는 **"조회 API 목록에 나오지 않는다"** 하나로 한정된다

### 10.6. 없는 id와 비활성 id를 코드로 구분하지 않음

- **기각한 대안:** `INGREDIENT_NOT_FOUND`와 `INGREDIENT_INACTIVE`를 나눔
- **기각 이유:** §10.5로 비활성은 실패가 아니게 됐고, 남은 실패는 하나다. 그리고 `data.code`는 앱이 분기할 것만 둔다는 것이 저장소 규칙이다

### 10.7. 시드는 Flyway migration에 INSERT

**CSV 리소스 + 기동 시 upsert로 바꾸지 말 것.**

- **기각한 대안:** `resources/seed/ingredients.csv` + 기동 시 upsert
- **기각 이유:** 파서·시드 서비스·초기화 훅과 그 테스트만큼 코드가 늘고, 기동 순서 함정이 생기며, `@DataJpaTest` 슬라이스에서는 시드가 돌지 않아 테스트와 운영 환경이 갈린다. **CSV도 jar 안에 들어가므로 목록 변경에 재배포가 필요한 것은 양쪽이 같다** — CSV가 주장하던 이점이 실제로는 없다. 이미 Flyway가 스키마의 진실을 소유하므로 데이터도 같은 파이프라인에 태우는 편이 일관된다
- **전환 시점:** PM이 목록을 주 단위로 갱신해 매번 SQL을 쓰는 것이 병목이 되면 그때 옮긴다. `code` 자연키가 있어 이전이 쉽다

### 10.8. `recipe_ingredient.ingredient_id`에 FK를 건다

- 저장소에는 FK가 있는 참조(`recipe_ingredient.recipe_id` 등)와 없는 참조(`cook_history.recipe_id`, `recipe.user_id`, `upload_object.user_id`)가 섞여 있다. 없는 쪽은 **의도한 설계가 아니다** — `cooking.md` §3.4가 *"스칼라 컬럼에는 JPA가 FK를 만들지 않는다. FK는 migration 도구 도입(#11) 시점에 추가한다"* 고 적어뒀고 #11에서 누락됐다([#23](https://github.com/swyp-app-6-team-2/backend/issues/23))
- 도메인 경계 규칙(다른 도메인의 JPA Entity를 참조하지 않는다)은 **자바 코드**에서 스칼라 id로 지키고, DB 무결성은 FK로 지킨다. 두 축은 충돌하지 않는다
- **기각한 대안:** `cook_history.recipe_id` 선례를 따라 FK를 두지 않음
- **기각 이유:** `cook_history`가 FK를 미룬 이유는 Recipe 삭제 시 잠금·삭제 순서 때문이다. `ingredient`는 **삭제하지 않는 마스터**라(§10.5의 `active`) 그 문제가 발생하지 않는다. `recipe_ingredient` INSERT는 참조된 `ingredient` 행에 공유 락만 걸고 공유 락끼리는 경합하지 않는다
- FK는 최후의 방어선이지 1차 검증이 아니다. 없는 id는 §8.3이 먼저 `400`으로 거른다

### 10.9. `display_order` 컬럼을 두지 않음

- **기각한 대안:** 카테고리 내 표시 순서를 컬럼으로 저장
- **기각 이유:** `code`의 숫자 부분이 이미 PM 시트의 카테고리 내 순서다. 카테고리 간 순서만 따로 필요한데 그것은 enum 선언 순서로 표현된다(§7.3). 컬럼이 필요해지는 것은 순서를 중간에 끼워 넣어야 할 때인데, 그런 요구가 아직 없다

### 10.10. 응답은 평탄한 배열, 카테고리로 묶지 않음

- **기각한 대안:** `categories[].ingredients[]` 중첩 + `categoryName` 한글 라벨
- **기각 이유:** 이 화면의 핵심 기능이 검색인데(기능명세서 F-06 *"카테고리별 대표 식재료 리스트 제공 및 검색창 지원"*) 검색 결과는 그룹이 아니라 평탄한 목록이다. 앱은 어차피 평탄한 배열을 갖게 된다. 소비 화면이 둘인데(F-06 재료 관리, F-13 레시피 재료 필터) 구조가 서로 다를 수 있고, 평탄한 배열은 둘 다 커버한다. 카테고리 순서는 이미 배열 순서로 전달된다. 그리고 서버가 `육류` 같은 화면 문구를 소유하지 않게 된다
- `categoryName`이 필요해지면 응답에 필드를 추가하면 된다. 필드 추가는 하위호환이다

---

## 11. Open Questions

셋 다 별도 이슈로 분리돼 있고 **이 구현을 막지 않는다.**

### 11.1. `name`의 최종 표기 — [#22](https://github.com/swyp-app-6-team-2/backend/issues/22)

87개 중 13개가 `돼지고기(삼겹살)`, `파프리카/피망` 같은 표기다. 그대로 쓸지(A안), 표시명을 짧게 하고 원본을 별칭으로 보낼지(B안)를 PM팀과 논의 중이다. 재료 칩 UI(F-13)에 아이콘과 함께 들어가서 길이가 디자인에 영향을 준다.

**막지 않는 이유:** 스키마·API·검증 로직이 `name` 값과 무관하다. 답이 오면 아직 머지되지 않은 migration의 값만 고치면 된다.

**출시 전에는 반드시 확정해야 한다.** `recipe_ingredient.name`이 스냅샷이라 사용자가 레시피를 저장하기 시작하면 그 시점 표기가 사용자 데이터에 영구히 남는다.

### 11.2. 아이콘 전달 방식 — [#24](https://github.com/swyp-app-6-team-2/backend/issues/24)

앱 번들에 포함할지(`MET001.png`), 서버가 `iconUrl`을 내려줄지 FE·Discovery와 논의 중이다. 아이콘 파일 자체를 아직 받지 못했다 — PM 시트의 `emoji` 컬럼이 87행 모두 비어 있다.

**막지 않는 이유:** 마스터에 아이콘 컬럼을 두지 않았다. 서버가 내려주는 쪽으로 결정되면 응답에 `iconUrl` 필드를 추가하면 되고, 필드 추가는 하위호환이다. 앱은 이미 `code`로 아이콘을 찾을 수 있다.

### 11.3. 다른 누락 FK — [#23](https://github.com/swyp-app-6-team-2/backend/issues/23)

`cook_history.recipe_id`, `recipe.user_id`, `upload_object.user_id`에도 FK가 없다. 뒤의 둘은 테스트가 `users` 행 없이 임의의 `ownerId`로 Recipe를 저장하고 있어 FK를 걸면 통합 테스트가 깨진다.

**이 범위 밖이다.** 이번에는 `recipe_ingredient.ingredient_id` FK만 추가한다.

---

## 12. 구현과 함께 해야 하는 것 — 로컬 dev 콘솔

**이것은 후속 작업이 아니라 구현 범위다.** `CLAUDE.md` §11·§12가 *"커밋을 제안하기 전에 로컬 Dev 콘솔에 반영할 것이 있는지 점검한다"* 를 규칙으로 두고 있고, 콘솔 파일은 `.git/info/exclude` 대상이라 고쳐도 커밋 내용이 달라지지 않는다. 미룰 이유가 없다.

`src/main/resources/static/dev/index.html`의 재료 마스터 패널은 **2026-09-09에 롤백된 설계로 만들어져 있어 이 스펙대로 구현하면 동작하지 않는다.**

| 현재 코드 | 문제 |
|---|---|
| `ingredientCatalog = res.json.data.categories;` | 응답이 §7.2의 평탄한 배열(`data.ingredients`)이라 `undefined`가 된다. 이어지는 `.reduce`에서 TypeError |
| `${it.iconEmoji ?? '  '}` | `iconEmoji`는 롤백된 필드다. 이 스펙의 응답에 없다 |
| 힌트 문구 *"기동 시 `seed/ingredients.csv` 87건을 upsert 한다"* | §10.7에서 기각한 적재 방식이다 |

평탄한 배열을 카테고리별로 묶어 렌더링하도록 고치고, `iconEmoji`를 제거하고, 문구를 Flyway migration 적재로 바꾼다. Recipe 폼의 `ingredientId` 입력칸은 이미 있어 그대로 쓴다.

## 13. 이 작업이 끝난 뒤 필요한 문서 동기화

구현 범위가 아니다. §8.2가 공개 계약을 바꾸므로 별도 작업으로 뒤따른다.

- 로컬 BE 문서 저장소 `02-1 Recipe API` — 요청·응답 재료 필드에 `ingredientId` 추가
- 로컬 BE 문서 저장소 `01-1 Recipe` ERD — `INGREDIENT`가 3컬럼(`id`, `name`, `category_code`)으로 그려져 있다. `code`·`aliases`·`active`를 반영
- 위 두 문서에 대응하는 Notion 페이지 (로컬과 Notion을 같은 작업에서 함께 수정)
- Notion FE-BE 공유 API 명세 DB에 `GET /api/v1/ingredients` 추가
- `docs/specs/recipe.md` — *"Ingredient와 Step의 내부 식별자와 표시 순서는 노출하지 않는다"* 문장에 `ingredientId` 예외 명시
